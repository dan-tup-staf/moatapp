# Postgres
POSTGRES_USER=moatapp
POSTGRES_PASSWORD=moatapp
POSTGRES_DB=moatapp

# JWT
JWT_SECRET=change-me-in-prod

# SMTP (dev: mailhog)
SMTP_HOST=mailhog
SMTP_PORT=1025

# Third-party API keys (optional — for future enrichment)
APOLLO_API_KEY=
HUNTER_API_KEY=
